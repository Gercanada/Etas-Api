import { DataTypes } from 'sequelize';
import db from '../db/connection';


const Attachment = db.define('etas', {
    /* 
    user_id: {
        type: DataTypes.INTEGER,
    },
    */
});
export default Attachment;