import { DataTypes } from 'sequelize';
import db from '../db/connection';


const PaymentIntent = db.define('etas', {
    /* 
    user_id: {
        type: DataTypes.INTEGER,
    },
    */
});
export default PaymentIntent;